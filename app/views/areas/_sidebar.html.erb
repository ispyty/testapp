<%- model_class = Store -%>
<div class="page-header">
  <h4>Cities in <%= params[:state] %></h4>
</div>

<ul class="nav nav-list">
  <li class="nav-header">Cities</li>
  <% @cities.each do |city| %>
    <li><%= link_to city, "/self-storage/" + params[:state] + "/" + city %></li>
  <% end %>
</ul>