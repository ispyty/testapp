<%= form_for @area, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :display_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :display_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :internal_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :internal_name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :area_type, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :area_type, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :latitude, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :latitude, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :longitude, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :longitude, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                areas_path, :class => 'btn' %>
  </div>
<% end %>
